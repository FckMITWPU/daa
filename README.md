# DAA IS EZ BABY
Some of the codes are AI generated(gemini pro 2.4) so be aware of anomalies and errors. \
If not working you can install gemini command line interface by firing this command. 
``` 
npm install -g @google/gemini-cli
```
after installation just enter 
```
gemini
```
in your command prompt and is should run like a charm 😉